[pytest]
testpaths = tests
pythonpath = .
# A sourced ROS shell puts its own pytest plugins on PYTHONPATH; they need packages this venv lacks.
addopts = -p no:launch_testing -p no:launch_ros -p no:launch_pytest
