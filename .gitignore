# colcon output, wherever it gets run from
build/
install/
log/

__pycache__/
*.pyc
*.egg-info/

# recorded demonstrations: video of people, not reproducible, keep a copy outside the repo
policy/datasets/

# secrets (API keys, tokens)
.env

# a Lambda rental (policy/lambda/): what comes home, and the state of the running rental
policy/outputs/
policy/logs/
policy/lambda/.rental
policy/lambda/.watchdog.lock
policy/lambda/PULL-FAILED-*
